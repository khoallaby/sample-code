const { __ } = wp.i18n;

// default settings
export const namespace           = 'everside';
export const blockCategory       = namespace;
export const blockComponentsCategory       = namespace + '-components';
export const ALLOWED_MEDIA_TYPES = [ 'image', 'svg' ];

export const bgColors = {
  'Light Green': 'very-light-green',
  'Light Purple': 'very-light-purple',
  'Light Blue': 'very-light-blue',
};

export const animationStyles = {
  'Fade in from bottom': 'fade-in-bottom',
  'Fade in from left': 'fade-in-left',
};
